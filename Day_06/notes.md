# Networking Day 6

Topic:
DNS

What is DNS?

DNS (Domain Name System) converts a domain name into an IP Address.

Example:

google.com
↓

142.xx.xx.xx

Commands:

cat /etc/resolv.conf

nslookup

dig

host

cat /etc/hosts
